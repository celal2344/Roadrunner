spring:
  application:
    name: roadrunner

  datasource:
    url: jdbc:mysql://localhost:3306/roadrunner
    username: root
    password: 3131
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100

springdoc:
  api-docs:
    path: /scalar/openapi
